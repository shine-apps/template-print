# 发布记录

> 每个版本一节，标题固定为 `## 版本号`（行尾可附日期）。发版时 CI 自动抽取对应段落写入 latest.json 与 GitHub Release。

## 0.3.0（2026-09-26）

- 模板复制重写：复制后先进入草稿态（不立即入库），图片资产在保存时自动复制并重新分配，副本完全自包含
- 图片元素：画布按原始比例 contain 渲染（与打印一致），缩放时锁定宽高比，插入时按图片比例初始化尺寸
- 文本垂直居中：画布排版与打印端对齐一致，内容超出元素框时从顶部开始、不再裁掉首行
- 设计器：网格间距 10mm→2mm，背景层缓存位图（大纸张拖拽不再卡顿），缩放改为 20%–300% 滑块
- 打印页：预览支持手动缩放、始终显示完整版式；仅打印文本（套打）模式下显示提示说明
- 未保存修改时切换导航或关闭窗口会弹出确认提示，避免误操作丢失编辑
- 打印页「调整版式」返回后保留已填写的参数值
- 日期参数格式新增月日、年月等 6 种格式
- 新增 MSI 安装包（企业 GPO/Intune 部署友好），自动更新改走 MSI 安装包

## 0.2.2（2026-09-26）

- 修复自动更新安装后仍显示旧版本的问题：NSIS 静默安装显式指定安装目录，确保新版本覆盖当前安装位置
- 应用名称改为中文「模板打印」：安装后 exe、桌面快捷方式、开始菜单、控制面板卸载项均显示中文名
- 窗口标题栏显示应用名与版本号（如「模板打印 v0.2.2」）

## 0.2.0（2026-09-26）

- 模板列表卡片展示真实版式缩略图（文本/图片/形状按比例渲染，参数占位符高亮）
- 隐藏应用默认菜单栏
- 数据备份扩展为 zip 压缩包：除数据库外，同时包含模板图片资产与应用设置
- 集成应用 Logo：窗口图标、任务栏、安装包、关于页与页面 favicon 统一使用品牌图标

## 0.1.2（2026-09-25）

- 修复重复启动会打开多个应用窗口的问题：再次启动时自动激活并前置已有窗口，窗口最小化时也会恢复显示

## 0.1.0（2026-09-25）

- 可视化模板设计：自定义纸张尺寸，文本/图片/直线/矩形/椭圆自由排版，撤销重做与图层管理
- 参数占位：文本中用 {{参数名称}} 引用，打印时自动填值；支持横排与竖排文本
- 仅打印文本（预印纸套打）、静默直打/弹框打印两种模式
- 打印历史与自动清理、数据库每日自动备份、模板导入导出（.tplx）
- 内置自动更新：每日检查、断点续传下载、SHA256 完整性校验、安装失败自动回滚
